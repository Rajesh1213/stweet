<%= form_for @tweet,:url => {:controller=> "tweets",:action => "create" },remote: true do |f| %>
    <div class="field">
      <label></label><br />
      <%= f.text_area :body%>
    </div>

    <div class="actions">
      <%= f.submit "TWEET",:class => "btn btn-success"%>
    </div>
<% end %>