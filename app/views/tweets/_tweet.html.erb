<li class="list-group-item <%= "tweet_#{tweet.id}"%>"><%= link_to tweet_path(tweet), method: :delete, id: tweet.id, data: { confirm: "Are you sure?", remote: true },:style=>"float:right" do %>
    <i class="glyphicon glyphicon-trash"></i>
<% end %> <%= tweet.body %></li>
